/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.application;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of defined application platforms, used to further classify an application record/resource
 */
@JsonDeserialize(using = ApplicationPlatform.Deserializer.class)
public class ApplicationPlatform extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * Identifies and application as being a device-native app.
    */
    public static ApplicationPlatform ApNative = new ApplicationPlatform("apNative");
    /**
     * Identifies an application for the Workpath platform
    */
    public static ApplicationPlatform ApWorkpath = new ApplicationPlatform("apWorkpath");
    /**
     * Identifies an application for the OXPd2 platform
    */
    public static ApplicationPlatform ApOXPd2 = new ApplicationPlatform("apOXPd2");

    protected static Hashtable<String, ApplicationPlatform> definitions;
    
    static {
        definitions = new Hashtable<String, ApplicationPlatform> ();
        definitions.put(ApNative.getValue(), ApNative);
        definitions.put(ApWorkpath.getValue(), ApWorkpath);
        definitions.put(ApOXPd2.getValue(), ApOXPd2);
    }

    private ApplicationPlatform() {
        this(null);
    }

    private ApplicationPlatform(String name) {
        this(name, name);
    }

    private ApplicationPlatform(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = ApplicationTypesManifest.getInstance().MajorVersion;
        typeName = "applicationPlatform";
        typeGUN = "com.hp.ext.types.application.version.1.type.applicationPlatform";
    }

    public static ApplicationPlatform Parse(String name)
    {
        ApplicationPlatform instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ApplicationPlatform(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ApplicationPlatform> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ApplicationPlatform deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ApplicationPlatform.Parse(node.asText());
        }
    }
}
