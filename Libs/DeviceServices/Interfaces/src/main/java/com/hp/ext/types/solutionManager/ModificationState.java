/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.solutionManager;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of the Solution Manager Solution status
 */
@JsonDeserialize(using = ModificationState.Deserializer.class)
public class ModificationState extends E2Enumeration {

    public static boolean isSealed() { return true; }

    /**
     * The modification is installed
    */
    public static ModificationState MsInstalled = new ModificationState("msInstalled");
    /**
     * The modification is currently being installed
    */
    public static ModificationState MsInstalling = new ModificationState("msInstalling");
    /**
     * The modification is currently being removed
    */
    public static ModificationState MsUninstalling = new ModificationState("msUninstalling");
    /**
     * The modification is uninstalled
    */
    public static ModificationState MsUninstalled = new ModificationState("msUninstalled");

    protected static Hashtable<String, ModificationState> definitions;
    
    static {
        definitions = new Hashtable<String, ModificationState> ();
        definitions.put(MsInstalled.getValue(), MsInstalled);
        definitions.put(MsInstalling.getValue(), MsInstalling);
        definitions.put(MsUninstalling.getValue(), MsUninstalling);
        definitions.put(MsUninstalled.getValue(), MsUninstalled);
    }

    private ModificationState() {
        this(null);
    }

    private ModificationState(String name) {
        this(name, name);
    }

    private ModificationState(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SolutionManagerTypesManifest.getInstance().MajorVersion;
        typeName = "modificationState";
        typeGUN = "com.hp.ext.types.solutionManager.version.1.type.modificationState";
    }

    public static ModificationState Parse(String name)
    {
        ModificationState instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ModificationState(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ModificationState> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ModificationState deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ModificationState.Parse(node.asText());
        }
    }
}
