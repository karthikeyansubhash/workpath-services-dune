/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.application;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of defined UI activity states.
 */
@JsonDeserialize(using = ActivityState.Deserializer.class)
public class ActivityState extends E2Enumeration {

    public static boolean isSealed() { return true; }

    /**
     * Identifies the sleep state.
    */
    public static ActivityState AsAsleep = new ActivityState("asAsleep");
    /**
     * Identifies the inactive state.
    */
    public static ActivityState AsInactive = new ActivityState("asInactive");
    /**
     * Identifies the awake-but-idle state.
    */
    public static ActivityState AsAwakeIdle = new ActivityState("asAwakeIdle");
    /**
     * Identifies an active state where the device is in use by a walk-up user.
    */
    public static ActivityState AsAwakeActive = new ActivityState("asAwakeActive");

    protected static Hashtable<String, ActivityState> definitions;
    
    static {
        definitions = new Hashtable<String, ActivityState> ();
        definitions.put(AsAsleep.getValue(), AsAsleep);
        definitions.put(AsInactive.getValue(), AsInactive);
        definitions.put(AsAwakeIdle.getValue(), AsAwakeIdle);
        definitions.put(AsAwakeActive.getValue(), AsAwakeActive);
    }

    private ActivityState() {
        this(null);
    }

    private ActivityState(String name) {
        this(name, name);
    }

    private ActivityState(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = ApplicationTypesManifest.getInstance().MajorVersion;
        typeName = "activityState";
        typeGUN = "com.hp.ext.types.application.version.1.type.activityState";
    }

    public static ActivityState Parse(String name)
    {
        ActivityState instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ActivityState(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ActivityState> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ActivityState deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ActivityState.Parse(node.asText());
        }
    }
}
