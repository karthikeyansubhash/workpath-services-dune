/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.service.device;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of alert visibility classifications.
 */
@JsonDeserialize(using = AlertVisibility.Deserializer.class)
public class AlertVisibility extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * The alert is intended to be consumed/used by all clients.
    */
    public static AlertVisibility AvAll = new AlertVisibility("avAll");
    /**
     * The alert is not intended to be consumed/used by the walk-up UI.
    */
    public static AlertVisibility AvNoFrontPanel = new AlertVisibility("avNoFrontPanel");
    /**
     * The alert is intended to only be consumed/used the walk-up UI.
    */
    public static AlertVisibility AvFrontPanelOnly = new AlertVisibility("avFrontPanelOnly");

    protected static Hashtable<String, AlertVisibility> definitions;
    
    static {
        definitions = new Hashtable<String, AlertVisibility> ();
        definitions.put(AvAll.getValue(), AvAll);
        definitions.put(AvNoFrontPanel.getValue(), AvNoFrontPanel);
        definitions.put(AvFrontPanelOnly.getValue(), AvFrontPanelOnly);
    }

    private AlertVisibility() {
        this(null);
    }

    private AlertVisibility(String name) {
        this(name, name);
    }

    private AlertVisibility(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = DeviceManifest.getInstance().MajorVersion;
        typeName = "alertVisibility";
        typeGUN = "com.hp.ext.service.device.version.1.type.alertVisibility";
    }

    public static AlertVisibility Parse(String name)
    {
        AlertVisibility instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  AlertVisibility(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<AlertVisibility> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public AlertVisibility deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return AlertVisibility.Parse(node.asText());
        }
    }
}
