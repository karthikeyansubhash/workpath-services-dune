/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.imaging;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * Enumeration of image preview mode/behavior.
 */
@JsonDeserialize(using = ImagePreviewMode.Deserializer.class)
public class ImagePreviewMode extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * Disables the native image preview functionality.
    */
    public static ImagePreviewMode IpmNone = new ImagePreviewMode("ipmNone");
    /**
     * Enables the standard native image preview functionality.
    */
    public static ImagePreviewMode IpmStandard = new ImagePreviewMode("ipmStandard");

    protected static Hashtable<String, ImagePreviewMode> definitions;
    
    static {
        definitions = new Hashtable<String, ImagePreviewMode> ();
        definitions.put(IpmNone.getValue(), IpmNone);
        definitions.put(IpmStandard.getValue(), IpmStandard);
    }

    private ImagePreviewMode() {
        this(null);
    }

    private ImagePreviewMode(String name) {
        this(name, name);
    }

    private ImagePreviewMode(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = ImagingTypesManifest.getInstance().MajorVersion;
        typeName = "imagePreviewMode";
        typeGUN = "com.hp.ext.types.imaging.version.1.type.imagePreviewMode";
    }

    public static ImagePreviewMode Parse(String name)
    {
        ImagePreviewMode instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ImagePreviewMode(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ImagePreviewMode> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ImagePreviewMode deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ImagePreviewMode.Parse(node.asText());
        }
    }
}
