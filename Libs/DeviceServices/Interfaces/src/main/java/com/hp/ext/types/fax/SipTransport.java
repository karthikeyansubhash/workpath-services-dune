/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.fax;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * Types used for SIP Transport
 */
@JsonDeserialize(using = SipTransport.Deserializer.class)
public class SipTransport extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * TCP
    */
    public static SipTransport StTCP = new SipTransport("stTCP");
    /**
     * UDP
    */
    public static SipTransport StUDP = new SipTransport("stUDP");
    /**
     * TLS
    */
    public static SipTransport StTLS = new SipTransport("stTLS");

    protected static Hashtable<String, SipTransport> definitions;
    
    static {
        definitions = new Hashtable<String, SipTransport> ();
        definitions.put(StTCP.getValue(), StTCP);
        definitions.put(StUDP.getValue(), StUDP);
        definitions.put(StTLS.getValue(), StTLS);
    }

    private SipTransport() {
        this(null);
    }

    private SipTransport(String name) {
        this(name, name);
    }

    private SipTransport(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = FaxTypesManifest.getInstance().MajorVersion;
        typeName = "sipTransport";
        typeGUN = "com.hp.ext.types.fax.version.1.type.sipTransport";
    }

    public static SipTransport Parse(String name)
    {
        SipTransport instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  SipTransport(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<SipTransport> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public SipTransport deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return SipTransport.Parse(node.asText());
        }
    }
}
