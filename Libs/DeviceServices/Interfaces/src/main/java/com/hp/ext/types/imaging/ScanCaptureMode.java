/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.imaging;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * Enumeration of scan capture modes.
 */
@JsonDeserialize(using = ScanCaptureMode.Deserializer.class)
public class ScanCaptureMode extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * The basic scan capture mode. Use this mode for scanning a single page or single document.
    */
    public static ScanCaptureMode ScmStandard = new ScanCaptureMode("scmStandard");
    /**
     * Job Build scan capture mode. This mode allows multiple documents/images to be scanned and incorporated into a single downstream job.
    */
    public static ScanCaptureMode ScmJobBuild = new ScanCaptureMode("scmJobBuild");
    /**
     * Book scan capture mode. Use this mode to enable the workflow for scanning a book.
    */
    public static ScanCaptureMode ScmBook = new ScanCaptureMode("scmBook");
    /**
     * ID-Card scan capture mode. Use this mode to enable the workflow for scanning a typical identification card.
    */
    public static ScanCaptureMode ScmIdCard = new ScanCaptureMode("scmIdCard");

    protected static Hashtable<String, ScanCaptureMode> definitions;
    
    static {
        definitions = new Hashtable<String, ScanCaptureMode> ();
        definitions.put(ScmStandard.getValue(), ScmStandard);
        definitions.put(ScmJobBuild.getValue(), ScmJobBuild);
        definitions.put(ScmBook.getValue(), ScmBook);
        definitions.put(ScmIdCard.getValue(), ScmIdCard);
    }

    private ScanCaptureMode() {
        this(null);
    }

    private ScanCaptureMode(String name) {
        this(name, name);
    }

    private ScanCaptureMode(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = ImagingTypesManifest.getInstance().MajorVersion;
        typeName = "scanCaptureMode";
        typeGUN = "com.hp.ext.types.imaging.version.1.type.scanCaptureMode";
    }

    public static ScanCaptureMode Parse(String name)
    {
        ScanCaptureMode instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ScanCaptureMode(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ScanCaptureMode> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ScanCaptureMode deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ScanCaptureMode.Parse(node.asText());
        }
    }
}
