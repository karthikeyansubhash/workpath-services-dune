/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.solutionManager;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of states related to the run-time initialization of the solution.
 */
@JsonDeserialize(using = InitializedState.Deserializer.class)
public class InitializedState extends E2Enumeration {

    public static boolean isSealed() { return true; }

    /**
     * The solution is fully initialized.
    */
    public static InitializedState IsSuccess = new InitializedState("isSuccess");
    /**
     * Some portion of solution initialization failed. Some aspects of the solution
         * may be appropriately configured, but at least one portion has failed. Device
         * logs must be consulted to determine the cause.
    */
    public static InitializedState IsPartialFailure = new InitializedState("isPartialFailure");

    protected static Hashtable<String, InitializedState> definitions;
    
    static {
        definitions = new Hashtable<String, InitializedState> ();
        definitions.put(IsSuccess.getValue(), IsSuccess);
        definitions.put(IsPartialFailure.getValue(), IsPartialFailure);
    }

    private InitializedState() {
        this(null);
    }

    private InitializedState(String name) {
        this(name, name);
    }

    private InitializedState(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SolutionManagerTypesManifest.getInstance().MajorVersion;
        typeName = "initializedState";
        typeGUN = "com.hp.ext.types.solutionManager.version.1.type.initializedState";
    }

    public static InitializedState Parse(String name)
    {
        InitializedState instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  InitializedState(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<InitializedState> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public InitializedState deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return InitializedState.Parse(node.asText());
        }
    }
}
