/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.supply;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * Enumeration of basic supply states.
 */
@JsonDeserialize(using = SupplyState.Deserializer.class)
public class SupplyState extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * Ok
    */
    public static SupplyState SsOk = new SupplyState("ssOk");
    /**
     * inform
    */
    public static SupplyState SsInform = new SupplyState("ssInform");
    /**
     * warning
    */
    public static SupplyState SsWarning = new SupplyState("ssWarning");
    /**
     * error
    */
    public static SupplyState SsError = new SupplyState("ssError");

    protected static Hashtable<String, SupplyState> definitions;
    
    static {
        definitions = new Hashtable<String, SupplyState> ();
        definitions.put(SsOk.getValue(), SsOk);
        definitions.put(SsInform.getValue(), SsInform);
        definitions.put(SsWarning.getValue(), SsWarning);
        definitions.put(SsError.getValue(), SsError);
    }

    private SupplyState() {
        this(null);
    }

    private SupplyState(String name) {
        this(name, name);
    }

    private SupplyState(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SupplyTypesManifest.getInstance().MajorVersion;
        typeName = "supplyState";
        typeGUN = "com.hp.ext.types.supply.version.1.type.supplyState";
    }

    public static SupplyState Parse(String name)
    {
        SupplyState instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  SupplyState(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<SupplyState> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public SupplyState deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return SupplyState.Parse(node.asText());
        }
    }
}
