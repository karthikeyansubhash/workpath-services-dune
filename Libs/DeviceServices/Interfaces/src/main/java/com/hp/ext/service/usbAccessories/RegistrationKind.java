/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.service.usbAccessories;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of registration kinds.
 */
@JsonDeserialize(using = RegistrationKind.Deserializer.class)
public class RegistrationKind extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * The accessories that match the registration are owned by the client.
    */
    public static RegistrationKind RkOwned = new RegistrationKind("rkOwned");
    /**
     * The accessories that match the registration are not owned by any client,
         * but are shared between them.
    */
    public static RegistrationKind RkShared = new RegistrationKind("rkShared");

    protected static Hashtable<String, RegistrationKind> definitions;
    
    static {
        definitions = new Hashtable<String, RegistrationKind> ();
        definitions.put(RkOwned.getValue(), RkOwned);
        definitions.put(RkShared.getValue(), RkShared);
    }

    private RegistrationKind() {
        this(null);
    }

    private RegistrationKind(String name) {
        this(name, name);
    }

    private RegistrationKind(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = UsbAccessoriesManifest.getInstance().MajorVersion;
        typeName = "registrationKind";
        typeGUN = "com.hp.ext.service.usbAccessories.version.1.type.registrationKind";
    }

    public static RegistrationKind Parse(String name)
    {
        RegistrationKind instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  RegistrationKind(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<RegistrationKind> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public RegistrationKind deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return RegistrationKind.Parse(node.asText());
        }
    }
}
