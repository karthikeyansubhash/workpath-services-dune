/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.solutionManager;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of reasons for a device shutdown
 */
@JsonDeserialize(using = ShutdownReason.Deserializer.class)
public class ShutdownReason extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * A normal power-off has been initiated.
    */
    public static ShutdownReason SrNormal = new ShutdownReason("srNormal");
    /**
     * A system reboot has been initiated.
    */
    public static ShutdownReason SrReboot = new ShutdownReason("srReboot");
    /**
     * The shutdown is occurring due to an error on the device.
    */
    public static ShutdownReason SrSystemError = new ShutdownReason("srSystemError");

    protected static Hashtable<String, ShutdownReason> definitions;
    
    static {
        definitions = new Hashtable<String, ShutdownReason> ();
        definitions.put(SrNormal.getValue(), SrNormal);
        definitions.put(SrReboot.getValue(), SrReboot);
        definitions.put(SrSystemError.getValue(), SrSystemError);
    }

    private ShutdownReason() {
        this(null);
    }

    private ShutdownReason(String name) {
        this(name, name);
    }

    private ShutdownReason(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SolutionManagerTypesManifest.getInstance().MajorVersion;
        typeName = "shutdownReason";
        typeGUN = "com.hp.ext.types.solutionManager.version.1.type.shutdownReason";
    }

    public static ShutdownReason Parse(String name)
    {
        ShutdownReason instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ShutdownReason(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ShutdownReason> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ShutdownReason deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ShutdownReason.Parse(node.asText());
        }
    }
}
