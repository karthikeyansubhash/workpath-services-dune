/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.service.supplies;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * Enumeration of anti-theft modes.
 */
@JsonDeserialize(using = AntiTheftMode.Deserializer.class)
public class AntiTheftMode extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * Undefined
    */
    public static AntiTheftMode AtmUndefined = new AntiTheftMode("atmUndefined");
    /**
     * Device
    */
    public static AntiTheftMode AtmDevice = new AntiTheftMode("atmDevice");
    /**
     * Fleet
    */
    public static AntiTheftMode AtmFleet = new AntiTheftMode("atmFleet");

    protected static Hashtable<String, AntiTheftMode> definitions;
    
    static {
        definitions = new Hashtable<String, AntiTheftMode> ();
        definitions.put(AtmUndefined.getValue(), AtmUndefined);
        definitions.put(AtmDevice.getValue(), AtmDevice);
        definitions.put(AtmFleet.getValue(), AtmFleet);
    }

    private AntiTheftMode() {
        this(null);
    }

    private AntiTheftMode(String name) {
        this(name, name);
    }

    private AntiTheftMode(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SuppliesManifest.getInstance().MajorVersion;
        typeName = "antiTheftMode";
        typeGUN = "com.hp.ext.service.supplies.version.1.type.antiTheftMode";
    }

    public static AntiTheftMode Parse(String name)
    {
        AntiTheftMode instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  AntiTheftMode(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<AntiTheftMode> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public AntiTheftMode deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return AntiTheftMode.Parse(node.asText());
        }
    }
}
