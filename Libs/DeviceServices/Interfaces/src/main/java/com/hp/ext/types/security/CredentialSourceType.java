/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.security;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration used to identify a credential source.
 */
@JsonDeserialize(using = CredentialSourceType.Deserializer.class)
public class CredentialSourceType extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * Indicates the source of the credentials is the current signed-in user's security context.
    */
    public static CredentialSourceType CstSecurityContext = new CredentialSourceType("cstSecurityContext");
    /**
     * Indicates the credentials are being provided directly in corresponding data structure.
    */
    public static CredentialSourceType CstProvided = new CredentialSourceType("cstProvided");

    protected static Hashtable<String, CredentialSourceType> definitions;
    
    static {
        definitions = new Hashtable<String, CredentialSourceType> ();
        definitions.put(CstSecurityContext.getValue(), CstSecurityContext);
        definitions.put(CstProvided.getValue(), CstProvided);
    }

    private CredentialSourceType() {
        this(null);
    }

    private CredentialSourceType(String name) {
        this(name, name);
    }

    private CredentialSourceType(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SecurityTypesManifest.getInstance().MajorVersion;
        typeName = "credentialSourceType";
        typeGUN = "com.hp.ext.types.security.version.1.type.credentialSourceType";
    }

    public static CredentialSourceType Parse(String name)
    {
        CredentialSourceType instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  CredentialSourceType(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<CredentialSourceType> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public CredentialSourceType deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return CredentialSourceType.Parse(node.asText());
        }
    }
}
