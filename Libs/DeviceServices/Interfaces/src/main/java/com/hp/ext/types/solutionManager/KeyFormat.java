/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.solutionManager;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of the support certificate private key formats
 */
@JsonDeserialize(using = KeyFormat.Deserializer.class)
public class KeyFormat extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * The key is formated in PKCS #8 standard format
    */
    public static KeyFormat KfPKCS8 = new KeyFormat("kfPKCS8");

    protected static Hashtable<String, KeyFormat> definitions;
    
    static {
        definitions = new Hashtable<String, KeyFormat> ();
        definitions.put(KfPKCS8.getValue(), KfPKCS8);
    }

    private KeyFormat() {
        this(null);
    }

    private KeyFormat(String name) {
        this(name, name);
    }

    private KeyFormat(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SolutionManagerTypesManifest.getInstance().MajorVersion;
        typeName = "keyFormat";
        typeGUN = "com.hp.ext.types.solutionManager.version.1.type.keyFormat";
    }

    public static KeyFormat Parse(String name)
    {
        KeyFormat instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  KeyFormat(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<KeyFormat> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public KeyFormat deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return KeyFormat.Parse(node.asText());
        }
    }
}
