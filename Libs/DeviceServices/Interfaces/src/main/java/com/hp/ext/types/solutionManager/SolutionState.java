/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.types.solutionManager;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * An enumeration of the Solution Manager Solution status.
 */
@JsonDeserialize(using = SolutionState.Deserializer.class)
public class SolutionState extends E2Enumeration {

    public static boolean isSealed() { return true; }

    /**
     * The solution is installed and functioning.
    */
    public static SolutionState SsInstalled = new SolutionState("ssInstalled");
    /**
     * The solution is currently being installed.
    */
    public static SolutionState SsInstalling = new SolutionState("ssInstalling");
    /**
     * The solution is currently being removed.
    */
    public static SolutionState SsUninstalling = new SolutionState("ssUninstalling");
    /**
     * The solution is not installed.
    */
    public static SolutionState SsUninstalled = new SolutionState("ssUninstalled");
    /**
     * The solution has been installed but is not in an active state.
    */
    public static SolutionState SsDeactivated = new SolutionState("ssDeactivated");

    protected static Hashtable<String, SolutionState> definitions;
    
    static {
        definitions = new Hashtable<String, SolutionState> ();
        definitions.put(SsInstalled.getValue(), SsInstalled);
        definitions.put(SsInstalling.getValue(), SsInstalling);
        definitions.put(SsUninstalling.getValue(), SsUninstalling);
        definitions.put(SsUninstalled.getValue(), SsUninstalled);
        definitions.put(SsDeactivated.getValue(), SsDeactivated);
    }

    private SolutionState() {
        this(null);
    }

    private SolutionState(String name) {
        this(name, name);
    }

    private SolutionState(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = SolutionManagerTypesManifest.getInstance().MajorVersion;
        typeName = "solutionState";
        typeGUN = "com.hp.ext.types.solutionManager.version.1.type.solutionState";
    }

    public static SolutionState Parse(String name)
    {
        SolutionState instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  SolutionState(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<SolutionState> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public SolutionState deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return SolutionState.Parse(node.asText());
        }
    }
}
