/***********************************************************
 * (C) Copyright 2022 HP Development Company, L.P.
 * All rights reserved.
 *
 * This file is automatically generated. DO NOT EDIT.
 ***********************************************************/
package com.hp.ext.service.jobStatistics;

import com.hp.ext.types.common.E2Enumeration;

import java.io.IOException;
import java.util.Hashtable;

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;

/**
 * A classification of types of impressions
 */
@JsonDeserialize(using = ImpressionClassification.Deserializer.class)
public class ImpressionClassification extends E2Enumeration {

    public static boolean isSealed() { return false; }

    /**
     * A (potential) impression requiring zero colorants (i.e., no marks are impressed on the media sheet 
         *  side by the print device).
         * 
         * Pre-printed media do not affect impression classification (i.e., it is the lack of any content 
         *  marked by the print device that distinguishes a Blank Impression).
         * 
         * Devices will count all sides/impressions that could have had marking agent applied but did 
         *  not (i.e. the side facing the ink head or the drum). Given this, devices will NOT count the 
         *  back side of a simplex sheet as a BlankSide.
         * 
         * For copy jobs, a high BackgroundCleanup setting may result in a BlankSide being printed for 
         *  a very light scanned image (e.g. a blank side in a duplex scan). In this case, the result from 
         *  one device model to another may vary.
    */
    public static ImpressionClassification IcBlank = new ImpressionClassification("icBlank");
    /**
     * An impression using only black (i.e. K) marking agent.
         * 
         * Pre-printed media do not affect impression classification.
         * 
         * On some devices, an impression that uses a trace amount of color ink (defined by a firmware-based 
         *  threshold that cannot be changed by a solution/customer, ink nozzle replacement or other print 
         *  bar servicing operations, for example) will be counted/classified as monochrome. The device may 
         *  or may not include the trace amount of color ink actually used in the job usage data.
         * 
         * If an impression uses more than a trace amount of color ink (even if the output appears to be monochrome, 
         *  same threshold as above, composite black is one example), those devices will count/classify the 
         *  impression as color and provide actual ink usage data.
    */
    public static ImpressionClassification IcMonochrome = new ImpressionClassification("icMonochrome");
    /**
     * An impression using any amount of any non-black marking agent (e.g. CMY).
         * 
         * Pre-printed media do not affect impression classification.
         * 
         * On some devices, an impression that uses a trace amount of color ink (defined by a firmware-based 
         *  threshold that cannot be changed by a solution/customer, ink nozzle replacement or other print bar 
         *  servicing operations, for example) will be counted/classified as monochrome. The device may or may 
         *  not include the trace amount of color ink actually used in the job usage data.
         * 
         * If an impression uses more than a trace amount of color ink (even if the output appears to be 
         *  monochrome, same threshold as above, composite black is one example), those devices will count/classify 
         *  the impression as color and provide actual ink usage data.
    */
    public static ImpressionClassification IcColor = new ImpressionClassification("icColor");
    /**
     * No impression. None is different from Blank in that the side never faced a marking mechanism 
         *  (i.e. the ink head or the drum).
         * 
         * Pre-printed media do not affect Impression classification.
         * 
         * Devices will NOT count non-impressed sides.
    */
    public static ImpressionClassification IcNone = new ImpressionClassification("icNone");

    protected static Hashtable<String, ImpressionClassification> definitions;
    
    static {
        definitions = new Hashtable<String, ImpressionClassification> ();
        definitions.put(IcBlank.getValue(), IcBlank);
        definitions.put(IcMonochrome.getValue(), IcMonochrome);
        definitions.put(IcColor.getValue(), IcColor);
        definitions.put(IcNone.getValue(), IcNone);
    }

    private ImpressionClassification() {
        this(null);
    }

    private ImpressionClassification(String name) {
        this(name, name);
    }

    private ImpressionClassification(String name, String jsonLiteral) {
        super(name, jsonLiteral);
        majorVersion = JobStatisticsManifest.getInstance().MajorVersion;
        typeName = "impressionClassification";
        typeGUN = "com.hp.ext.service.jobStatistics.version.1.type.impressionClassification";
    }

    public static ImpressionClassification Parse(String name)
    {
        ImpressionClassification instance = definitions.get(name);
        if (instance == null && !isSealed()) {
            instance = new  ImpressionClassification(name);
        }
        return instance;
    }

    public static class Deserializer extends StdDeserializer<ImpressionClassification> { 

        public Deserializer() { 
            this(null); 
        } 
    
        public Deserializer(Class<?> vc) { 
            super(vc); 
        }
    
        @Override
        public ImpressionClassification deserialize(JsonParser jp, DeserializationContext ctxt) 
            throws IOException, JsonProcessingException {
            ObjectMapper mapper = (ObjectMapper) jp.getCodec();
            JsonNode node = mapper.readTree(jp);
            
            return ImpressionClassification.Parse(node.asText());
        }
    }
}
