<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <uses-permission android:name="${SERVICES_PERMISSION}" />

    <application>
        <provider
            android:name="com.hp.jetadvantage.link.services.scanlet.provider.ScanletContentProvider"
            android:authorities="com.hp.jetadvantage.link.authority.oxp.scanletcp"
            android:exported="true"
            tools:ignore="Instantiatable" />

        <provider android:name="com.hp.jetadvantage.link.services.scanlet.provider.ScanFileContentProvider"
            android:authorities="com.hp.jetadvantage.link.authority.oxp.scanletcp"
            android:exported="true"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>

        <receiver
            android:name="com.hp.jetadvantage.link.services.scanlet.receiver.ScanletBroadcastReceiver">
            <intent-filter android:priority="1">
                <action android:name="com.hp.jetadvantage.link.intent.action.SCANTO" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <service android:name="com.hp.jetadvantage.link.services.scanlet.service.ScanJobIntentService"
            android:permission="${SERVICES_PERMISSION}"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hp.jetadvantage.link.intent.action.BASIC_API_CANCEL_SCAN" />
            </intent-filter>
        </service>
    </application>

</manifest>
