<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <uses-permission android:name="${SERVICES_PERMISSION}" />
    <uses-permission android:name="${DISABLE_PRINTING_PORTS_PERMISSION}" />

    <application>
        <provider
            android:name="com.hp.jetadvantage.link.services.devicesettingslet.provider.DeviceSettingsLetContentProvider"
            android:authorities="com.hp.jetadvantage.link.authority.devicesettingstcp"
            android:exported="true"/>
    </application>

</manifest>
